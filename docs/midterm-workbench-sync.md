# 中线股票名单同步

现行范围：只接入《中线交易系统规划》明确交付的股票名单及个股相关内容。排除用户此前粘贴的调度规划、系统规则、回测、任务时间表、账户数据。用户已授权写入iroryagame-afk/ai和无需登录CSNPK，保持不进入公共导航。

每日北京时间17:40，既有自动化a-6检查源名单，有变化才发布，无变化静默。仅更新内容，不执行交易任务或重新扫描。

先通过read_thread读取local/01a0847f-38e6-7430-8507-ad77bd310cac，定位最新明确交付路径。当前名单为行业分析工作区outputs/a_stock_plan_20260909/股票规划.md。不得用同目录旧名单或调度规划替代；源线程内容缺失时可只读对应会话记录定位最新交付。

运行 scripts/build_midterm_system_data.py --source <名单文件> --out <临时输出>。仅输出角色、股票代码名称、产业、管理窗口、源日期收盘、模型状态、下一步及替补。结构变更时复核适配字段；不猜测数据、不改变研究结论、不读取账户字段。

与origin/main的a-share-workbench/midterm.json比较；无变化不运行浏览器或部署。保留真实源日期，不用同步时间冒充行情时间。缺源或源并发写入保留旧版。

遵守docs/csnpk_refresh_runbook.md：最新origin/main干净隔离worktree；常规只改midterm.json，导出器适配单列。完成语法、内容、单一浏览器桌面和移动QA后PR合并；合并提交单次部署，缓存穿透回读JSON/JS的SHA-256及HTML。Cloudflare注入的分析脚本可单独剔除后核对业务HTML，不能把业务差异当缓存忽略。不得恢复旧规划正文，不修改其他栏目或Server酱任务。
