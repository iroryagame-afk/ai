# A股交易工作台

用户限定：独立个人入口 /a-share-workbench/，不加入首页、共享导航或报告目录。用户最新明确不启用访问保护：采用未列入导航的公开链接，noindex只表达不索引意愿，任何获得链接的人均可访问。持仓截图与账户信息不得提交或上传。内容变更型页面，不纳入CN收盘门禁，不修改原云端、Server酱或A观察任务。

## 内容来源
- 公开data.json是白名单导出的发布快照。不是实时行情或当前账户成员。
- 盘前与盘后提供已有云端对话入口、本机纯文本导入；没有后台云端抓取器。
- 持仓图片和备注只写当前浏览器localStorage，不上传、不自动识别、不跨设备同步。
- 盘中任务入口使用Codex任务链接；普通浏览器/手机未安装Codex时未必能打开。页面仍显示发布状态快照。

## 更新
在最新origin/main隔离工作树执行：
`python3 scripts/build_a_share_workbench_data.py --workspace <股票工作区>`
脚本当前显式读取已核验的20260909 A观察复核文件及a30最新状态。更换观察批次前核验模型版本、数据日期和写后回读；本脚本不扫描、不推送、不写账户。不要把该固定批次当成自动最新源。
运行共享导航、manifest、report-nav、导航测试及JS语法检查，然后提交、推送、单次部署、公开哈希与视觉回读。

## 已完成验证
本地桌面与390px移动端；名单搜索；本机备注保存后刷新保留及清除；报告导入纯文本显示（含HTML注入样例）；未上传真实持仓。共享导航18项测试通过。

## 后续接入
云端报告稳定交接、A观察固定刷新频率、盘后实际时间冲突均未解决。本页明确显示这些状态，不冒充自动同步。任何持仓同步到服务器的扩展需另行决定私有访问与授权。

## 2026-09-09 页内阅读修订

早报与复盘从已登录云端任务的实际报告读取，整理至 `a-share-workbench/reports.json`，保留报告面向交易日、冻结时间、取得日期及原任务来源。当前两份为2026-09-08报告；市场数据未在本轮重新核验。整理版不是完整逐字转载。读取盘后正文时跳过个人持仓诊断，不在公开JSON中保存该部分。

前端直接渲染标题、段落、表格，所有正文通过textContent生成，不接受报告HTML。云端报告尚未无人值守同步；后续更新须重新读取实际输出、排除个人信息并保留日期。候选名单直接读取同站 `/a-share-trend-candidates/data.json`，不复制调度或触发扫描。A观察和半小时状态维持明确的历史快照标签。本站导航、首页和访问控制不变，持仓继续仅本机保存。


2026-09-09 用户确认“模型趋势候选”为旧版本：已从工作台总览移除模块及对旧候选JSON的读取。以后不得自动重新接入；原独立页面不在本次删除范围。
